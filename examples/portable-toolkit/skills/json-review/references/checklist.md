# JSON review checklist

- Confirm whether the root is an object, array, or scalar.
- Check the requested keys and types against the consuming application's needs.
- JSON syntax validation does not validate an application's schema.
- Keep the file unchanged when the task is inspection only.
